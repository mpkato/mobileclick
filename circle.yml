machine:
    python:
        version: 2.7.9
dependencies:
    pre:
        - pip install --upgrade setuptools
test:
    pre:
        - python -m nltk.downloader maxent_treebank_pos_tagger punkt stopwords
        - nosetests tests/scripts_test.py
general:
    artifacts:
        - "runs"
